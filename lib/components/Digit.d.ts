import React from 'react';
import type { KeyedDigitPart } from '../types';
type Props = {
    part: KeyedDigitPart;
    testID?: string;
};
/** A single digit column that spins through its glyphs to reach each new value. */
export declare function Digit({ part, testID }: Props): React.JSX.Element;
export {};
//# sourceMappingURL=Digit.d.ts.map
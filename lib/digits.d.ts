import type { Digits } from './types';
/** Number of glyphs in the column for a digit position, honoring `digits[pos].max`. */
export declare function getColumnLength(digits: Digits | undefined, pos: number): number;
//# sourceMappingURL=digits.d.ts.map